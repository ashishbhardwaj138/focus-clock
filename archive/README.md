# Archived files

This directory contains superseded installer and uninstaller scripts from the early Focus Clock setup flow.

They are retained for historical reference only. Do not run them.

The only runnable application files in the project root are:

- `FocusClock.cmd` â€” double-click launcher
- `FocusClock.ps1` â€” the identical application implementation launched by the CMD file

